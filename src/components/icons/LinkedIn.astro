---
interface Props {
  class?: string;
}
const { class: className = 'w-5 h-5' } = Astro.props;
import linkedinLogo from './linkedin.svg';
---

<img src={linkedinLogo.src} alt="LinkedIn" class={className} />
