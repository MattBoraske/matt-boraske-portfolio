---
interface Props {
  title: string;
  icon?: string;
}

const { title, icon = '🔹' } = Astro.props;
---

<div class="px-6 py-4 bg-[var(--color-bg-secondary)] rounded-xl border border-[var(--color-border)] hover:border-[var(--color-accent)]/50 transition-colors">
  <span class="text-2xl mr-3">{icon}</span>
  <span class="text-[var(--color-text)] font-medium">{title}</span>
</div>
